version = 1
mode = "server"
strict = true

[flags2env]
contract = ".cli-flags.toml"
require_audit = true
precedence = "argv-over-env"

[server]
enabled = true
bind_addr_binding = "bind_addr"
database_url_binding = "database_url"
push_provider_binding = "push_provider"

[[env]]
name = "bind_addr"
key = "FANWAAVE_BIND_ADDR"
kind = "string"
required = false
secret = false
default = "0.0.0.0:8080"

[[env]]
name = "database_url"
key = "DATABASE_URL"
kind = "url"
required = true
secret = true
description = "Database credential URI from the approved runtime secret boundary."

[[env]]
name = "push_provider"
key = "FANWAAVE_PUSH_PROVIDER"
kind = "string"
required = false
secret = false
default = "fcm"
